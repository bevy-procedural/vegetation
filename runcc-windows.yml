commands:
  bin: |
    cargo watch -w editor/src -w components -x "run -p editor --features reload --target-dir target-bin"
  systems: |
    cargo watch -w src -w components -x "build --features dynamic"
